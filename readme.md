# Google Cloud Platform Pugin for Google Chrome

## Features 

- Adds a link next to containers to copy `kubectl exec` command to clipboard
  ![Copy exec command screenshot](./doc/copy-kubectl-exec-command.png)